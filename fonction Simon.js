// for = (déclaration ; condition ; action de fin)

function monNom(nIteration, nom)
{
for(i = 0 ; i<= nIteration ; i++)
    {
    message = "Je m'appelle " + nom + "i : " + i

    }
}

function testMultiple(i)
{
    newMessage = " " ;
    if(i%3 == 0)
    newMessage + " Fizz"

    newMessage = " " ;
    if(i%5 == 0)
    newMessage + " Buzz"
}